# Copy to .env.sync and fill with your server values
# This file is NOT used by app runtime, only by sync scripts.

SYNC_HOST=your.server.ip.or.hostname
SYNC_USER=root
SSH_PORT=22
REMOTE_DIR=/opt/photobooksgallery
