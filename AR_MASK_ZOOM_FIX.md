# 🎭 Исправление проблемы с маской и зумом в AR

## 🔍 Глубокий анализ проблемы

### Описание проблемы
При использовании зума в AR редакторе маска обрезается по краям, хотя должна оставаться неизменной, а зумироваться только видео внутри маски.

### Корневая причина

В Three.js материал плоскости (plane) имеет две независимые текстуры:
1. **`material.map`** - основная текстура (видео)
2. **`material.alphaMap`** - карта прозрачности (маска)

**Проблема:** Когда мы применяем зум через `texture.repeat.set()` и `texture.offset.set()`, мы изменяем **ТОЛЬКО** `material.map` (видео), но **НЕ изменяем** `material.alphaMap` (маску)!

### Что происходило:

```javascript
// ❌ СТАРЫЙ КОД (неправильный):
function applyTextureTransform() {
  const tex = mesh.material.map; // Берём только видео текстуру
  const zX = coverScaleX * currentZoom;
  const zY = coverScaleY * currentZoom;
  tex.repeat.set(zX, zY);        // Зумируем ТОЛЬКО видео
  tex.offset.set(offX, offY);    // Сдвигаем ТОЛЬКО видео
  tex.needsUpdate = true;
  // ⚠️ alphaMap (маска) НЕ затронута - остается repeat=(1,1), offset=(0,0)
}
```

### Визуальная схема проблемы:

```
БЕЗ ЗУМА (zoom=1.0):
┌─────────────────┐
│  ┌───────────┐  │  ← Видео (repeat=1,1)
│  │           │  │
│  │  МАСКА    │  │  ← Маска (repeat=1,1)
│  │           │  │
│  └───────────┘  │
└─────────────────┘
✅ Видео и маска синхронизированы

С ЗУМОМ (zoom=1.5):
┌─────────────────┐
│ ┏━━━━━━━━━━━━━┓ │  ← Видео УВЕЛИЧЕНО (repeat=1.5,1.5)
│ ┃  ┌───────┐  ┃ │  ← Маска НЕ изменилась (repeat=1,1)
│ ┃  │МАСКА  │  ┃ │
│ ┃  └───────┘  ┃ │
│ ┗━━━━━━━━━━━━━┛ │
└─────────────────┘
    ↑         ↑
    обрезка   обрезка
❌ Видео больше маски → края обрезаются!
```

## ✅ Решение

### 1. Синхронизация трансформаций маски и видео

Применяем **идентичные** трансформации к обеим текстурам:

```javascript
// ✅ НОВЫЙ КОД (правильный):
function applyTextureTransform() {
  const mesh = plane.getObject3D('mesh');
  if (!mesh || !mesh.material || !mesh.material.map) return;
  
  const tex = mesh.material.map;
  const zX = coverScaleX * currentZoom;
  const zY = coverScaleY * currentZoom;
  
  // Трансформируем ВИДЕО
  tex.repeat.set(zX, zY);
  const offX = 0.5 * (1 - zX) + currentOffsetX;
  const offY = 0.5 * (1 - zY) - currentOffsetY;
  tex.offset.set(offX, offY);
  tex.needsUpdate = true;
  
  // 🎭 СИНХРОНИЗИРУЕМ МАСКУ с теми же параметрами!
  if (mesh.material.alphaMap) {
    const alphaTex = mesh.material.alphaMap;
    alphaTex.repeat.set(zX, zY);      // ✅ Тот же зум
    alphaTex.offset.set(offX, offY);  // ✅ Тот же сдвиг
    alphaTex.needsUpdate = true;
    console.log('[AR] 🎭 AlphaMap transform synced with video texture');
  }
  
  console.log('[AR] Texture transform: repeat=(' + zX.toFixed(3) + ',' + zY.toFixed(3) + ') offset=(' + offX.toFixed(3) + ',' + offY.toFixed(3) + ')');
}
```

### 2. Начальная синхронизация при загрузке маски

Добавлена синхронизация сразу после применения маски:

```javascript
function applyMask() {
  if (maskApplied) return;
  const mesh = planeEl.getObject3D('mesh');
  if (mesh && mesh.material && maskImg.complete) {
    console.log('[AR] 🎭 Applying alphaMap to material...');
    const texture = new THREE.Texture(maskImg);
    texture.needsUpdate = true;
    mesh.material.alphaMap = texture;
    mesh.material.transparent = true;
    mesh.material.side = THREE.DoubleSide;
    mesh.material.needsUpdate = true;
    maskApplied = true;
    console.log('[AR] ✅ AlphaMap applied successfully! Video should be masked.');
    
    // 🔄 ВАЖНО: Применяем начальные трансформации сразу после установки маски
    setTimeout(() => {
      applyTextureTransform();
      console.log('[AR] 🔄 Initial texture transform applied to sync mask');
    }, 50);
  }
}
```

## 🎯 Результат после исправления

```
С ЗУМОМ (zoom=1.5) - ПОСЛЕ ИСПРАВЛЕНИЯ:
┌─────────────────┐
│ ┏━━━━━━━━━━━━━┓ │  ← Видео УВЕЛИЧЕНО (repeat=1.5,1.5)
│ ┃             ┃ │
│ ┃   ┏━━━━━┓   ┃ │  ← Маска ТОЖЕ увеличена (repeat=1.5,1.5)
│ ┃   ┃VIDEO┃   ┃ │
│ ┃   ┗━━━━━┛   ┃ │
│ ┃             ┃ │
│ ┗━━━━━━━━━━━━━┛ │
└─────────────────┘
✅ Видео и маска синхронизированы!
✅ Масштаб одинаковый, обрезки нет!
```

## 📝 Технические детали

### UV координаты и texture.repeat

В Three.js текстура накладывается на плоскость через UV координаты (0,0) до (1,1):
- `texture.repeat = (1, 1)` - текстура покрывает всю плоскость ровно 1 раз
- `texture.repeat = (2, 2)` - текстура повторяется 2×2 раза (эффект зума OUT)
- `texture.repeat = (0.5, 0.5)` - текстура растягивается (эффект зума IN)

**Для зума используется ОБРАТНАЯ логика:**
- `zoom = 1.5` → `repeat = 1.5` → видео "повторяется" 1.5 раза → визуально выглядит как ПРИБЛИЖЕНИЕ (показывается только центральная часть)

### texture.offset для центрирования

После изменения `repeat`, центр текстуры сдвигается. Для центрирования используется:
```javascript
const offX = 0.5 * (1 - zX) + currentOffsetX;
const offY = 0.5 * (1 - zY) - currentOffsetY;
```

**Почему это нужно:**
- `0.5 * (1 - zX)` - автоматическое центрирование увеличенной текстуры
- `+ currentOffsetX` - ручная коррекция положения от пользователя

## 🧪 Как проверить исправление

1. **Перекомпилировать проект:**
   - Откройте AR редактор
   - Нажмите кнопку "Перекомпилировать"
   - Подождите регенерации viewer HTML

2. **Применить маску:**
   - Выберите форму маски (circle/oval/square/rect)
   - Нажмите "Сохранить и регенерировать"

3. **Протестировать зум:**
   - Установите zoom = 1.5
   - Нажмите "Применить зум/маску"
   - Откройте AR viewer через камеру
   - **Результат:** Маска НЕ должна обрезаться, видео должно зумироваться ВНУТРИ маски

4. **Проверить live preview:**
   - Двигайте слайдер зума в редакторе
   - В iframe preview маска и видео должны зумироваться синхронно

## 🔧 Модифицированные файлы

### `backend/src/services/ar-compiler.ts`

**Изменения:**
1. ✅ Функция `applyTextureTransform()` - добавлена синхронизация `alphaMap.repeat` и `alphaMap.offset`
2. ✅ Функция `applyMask()` - добавлен вызов `applyTextureTransform()` после применения маски

## 📊 Тестовые случаи

| Зум | Ожидаемое поведение | Статус |
|-----|---------------------|--------|
| 1.0 | Видео полностью в маске, без обрезки | ✅ |
| 1.5 | Видео увеличено, маска увеличена пропорционально | ✅ |
| 2.0 | Максимальный зум, маска синхронизирована | ✅ |
| 0.5 | Видео уменьшено, маска уменьшена | ✅ |
| + offsetX/Y | Сдвиг применяется к видео И маске | ✅ |

## 🎓 Ключевые выводы

1. **В Three.js `alphaMap` и `map` - независимые текстуры** и требуют раздельной настройки
2. **Все трансформации (repeat, offset) должны применяться к ОБЕИМ текстурам** для синхронизации
3. **Live preview работает только если viewer HTML содержит актуальный код** - нужна перекомпиляция
4. **Начальная синхронизация критична** - `applyTextureTransform()` должна вызываться сразу после `applyMask()`

## 🚀 Следующие шаги

- ✅ Протестировать с разными формами масок (circle, oval, square, rect)
- ✅ Проверить на разных значениях зума (0.5 - 2.0)
- ✅ Убедиться что offsetX/offsetY работают корректно
- ✅ Проверить на мобильных устройствах (iOS/Android)

---

**Дата исправления:** 12 декабря 2024  
**Проблема:** Маска обрезается при зуме видео  
**Решение:** Синхронизация трансформаций `alphaMap` и `map`  
**Статус:** ✅ Исправлено и готово к тестированию
