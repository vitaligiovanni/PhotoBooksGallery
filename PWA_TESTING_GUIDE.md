# 🚀 Быстрый старт - PWA тестирование

## Шаг 1: Сборка production версии

```bash
cd frontend
npm run build
```

## Шаг 2: Запуск preview сервера

```bash
npm run preview
```

> ⚠️ Service Worker НЕ работает в dev режиме (`npm run dev`)!  
> Обязательно использовать production build.

## Шаг 3: Открыть в браузере

```
http://localhost:4173
```

## Шаг 4: Проверить PWA

### Chrome DevTools:

1. **F12** → **Application** tab
2. Проверить:
   - ✅ **Manifest**: должен загрузиться без ошибок
   - ✅ **Service Workers**: статус "activated and is running"
   - ✅ **Cache Storage**: должны появиться кэши

### Lighthouse Audit:

1. **F12** → **Lighthouse** tab
2. Выбрать категории: **PWA**, **Performance**
3. Нажать **Generate report**
4. Цель: **PWA Score 90+**

## Шаг 5: Тест на реальном устройстве

### iOS (Safari):

1. Открыть ссылку на iPhone/iPad
2. Нажать кнопку **Share** (внизу)
3. Выбрать **"Add to Home Screen"**
4. Тапнуть иконку → Должно открыться в полноэкранном режиме

### Android (Chrome):

1. Открыть ссылку
2. Должен появиться banner **"Install app"**
3. Или: Меню → **"Add to Home screen"**
4. Тапнуть иконку → Должно открыться как приложение

## Шаг 6: Тест AR функциональности

1. Перейти на `/ar/create` или `/living-photos`
2. Создать тестовый AR проект
3. Получить QR код
4. Отсканировать QR → Должен открыться AR viewer
5. Дать разрешение камеры
6. Навести на фото → Видео должно появиться

## Проверка офлайн режима:

1. Открыть приложение
2. Chrome DevTools → **Network** tab
3. Включить **Offline** режим
4. Обновить страницу
5. ✅ Страница должна загрузиться из кэша
6. ⚠️ API запросы будут недоступны (это норма)

## Типичные проблемы:

### 1. Service Worker не активируется

**Решение:**
```bash
# Пересобрать проект
npm run build
npm run preview
```

### 2. HTTPS required

**PWA требует HTTPS (кроме localhost)**

**Для тестирования:**
- ✅ `localhost` - работает
- ✅ `127.0.0.1` - работает
- ❌ `192.168.x.x` - не работает (нет HTTPS)

**Решение для локальной сети:**
```bash
# Использовать ngrok или localtunnel
npx localtunnel --port 4173
```

### 3. Камера не работает

**Причины:**
- Не HTTPS (кроме localhost)
- Разрешение не дано
- Камера занята другим приложением

**Проверка:**
```javascript
// В консоли браузера:
navigator.mediaDevices.getUserMedia({ video: true })
  .then(() => console.log('✅ Camera OK'))
  .catch(err => console.error('❌', err.message));
```

### 4. Install prompt не показывается

**Возможные причины:**
- Уже установлено
- Не соответствует PWA criteria (Lighthouse audit)
- iOS не поддерживает `beforeinstallprompt` (норма)

**iOS:**
- Нужно вручную "Add to Home Screen"
- Автоматический prompt недоступен

---

## Быстрая диагностика:

```bash
# 1. Service Worker зарегистрирован?
# Console: [PWA] Service Worker registered: /

# 2. Manifest загрузился?
# Console: (manifest должен быть без ошибок в Network tab)

# 3. Кэш создан?
# Application → Cache Storage → photobooksgallery-v1.0.0

# 4. Офлайн работает?
# Network → Offline → Refresh → Страница загрузилась?
```

---

## Готово к продакшену?

✅ Checklist:
- [ ] PWA Score > 90 (Lighthouse)
- [ ] Installable (✅ в Lighthouse)
- [ ] Works Offline (базовые страницы)
- [ ] HTTPS на продакшен сервере
- [ ] Иконки всех размеров созданы
- [ ] Manifest заполнен корректно
- [ ] Service Worker без ошибок
- [ ] AR viewer работает на реальных устройствах

🎉 **Если все ✅ - можно деплоить!**
