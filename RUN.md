Короткий чек-лист для стабильного запуска (Windows/PowerShell)

1) Запустить Docker Desktop
2) В папке проекта:
   - Запустить БД:
     - Задача VS Code: "🗄️ Запустить БД" и "🗄️ Запустить AR БД"
   - Проверить/установить зависимости:
     - `powershell`:
       ```powershell
       .\scripts\doctor.ps1
       ```
3) Запустить dev-серверы:
   - `npm run dev` (запустит фронт и бэк)
   - при необходимости отдельно:
     - Backend: `cd backend; npm run dev`
     - Frontend: `cd frontend; npm run dev`
4) AR-сервис (опционально):
   - Задача VS Code: "🎯 Запустить AR сервис"

Восстановление данных (если пропали товары):
- Импортируйте `restore-basic-data.sql` в локальную БД.
- Быстро: `.\scripts\doctor.ps1 -Seed` (попытка через контейнер) или вручную через `psql`.

Частые причины "сломалось после перезагрузки":
- Не запущен Docker Desktop → контейнеры БД недоступны.
- Удалились `node_modules` (чистка диска/кэш) → нужно `npm install` в корне и workspaces.
- Антивирус/индексация тормозят watcher → добавьте папку проекта в исключения.
- Неполные ENV переменные → используйте `.env`/`.env.local` и проверьте их.
