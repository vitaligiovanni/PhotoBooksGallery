# 🎈 AR Сервис - Инструкция для Начинающих

> **Представь:** У тебя есть большой дом (проект) с разными комнатами. Мы будем ходить из комнаты в комнату, делать там дела, и **обязательно возвращаться обратно**! 🏠

---

## 🗺️ Карта Нашего Дома (Проекта)

```
📁 photobooksgallery/                    ← 🏠 ГЛАВНЫЙ ДОМ (корень проекта)
   ├── 📁 frontend/                      ← 🎨 Комната с красивыми картинками
   ├── 📁 backend/                       ← 🧠 Комната с умными штуками
   ├── 📁 ar-service/                    ← 🎮 НОВАЯ КОМНАТА! AR волшебство здесь!
   │   ├── 📁 src/                       ← Код AR сервиса
   │   ├── 📄 package.json               ← Список игрушек (зависимости)
   │   └── 📄 .env                       ← Секретные настройки
   ├── 📁 nginx/                         ← 🚪 Вратарь (пропускает гостей)
   └── 📄 docker-compose.yml             ← 🎪 Инструкция как запустить всё
```

**Важно запомнить:**
- Когда открываешь PowerShell, ты стоишь в **ГЛАВНОМ ДОМЕ** (`photobooksgallery/`)
- Когда пишешь `cd ar-service`, ты **заходишь в комнату AR**
- Когда пишешь `cd ..`, ты **выходишь обратно в ГЛАВНЫЙ ДОМ**

---

## 🎯 Шаг 1: Установить Игрушки в AR Комнату

### Где мы сейчас? 
В **ГЛАВНОМ ДОМЕ** (`photobooksgallery/`)

### Что делаем?

```powershell
# 1️⃣ ЗАЙТИ В КОМНАТУ AR
cd ar-service
# Теперь ты ВНУТРИ ar-service! 📍 Адрес: photobooksgallery/ar-service/

# 2️⃣ УСТАНОВИТЬ ИГРУШКИ (программы, которые нужны)
npm install
# Это как скачать конструктор LEGO со всеми деталями
# Подожди 1-2 минуты... ⏳

# 3️⃣ ВЕРНУТЬСЯ В ГЛАВНЫЙ ДОМ!
cd ..
# Теперь ты снова в photobooksgallery/ 🏠
```

### Что установилось?
- `mind-ar` - волшебная программа для AR (дополненная реальность)
- `pg-boss` - помощник, который управляет очередью задач
- `express` - веб-сервер (принимает гостей-запросы)
- `sharp`, `canvas` - художники (обрабатывают картинки)
- `qrcode` - генератор QR кодов

**Проверка:** Должна появиться папка `ar-service/node_modules/` (там все игрушки хранятся)

---

## 🔐 Шаг 2: Настроить Секреты

### Где мы? 
В **ГЛАВНОМ ДОМЕ** (`photobooksgallery/`)

### Что делаем?

```powershell
# 1️⃣ ЗАЙТИ В КОМНАТУ AR
cd ar-service
# 📍 Теперь в: photobooksgallery/ar-service/

# 2️⃣ СКОПИРОВАТЬ ШАБЛОН СЕКРЕТОВ
Copy-Item .env.example .env
# Это как скопировать чистый дневник и начать его заполнять

# 3️⃣ ОТКРЫТЬ ФАЙЛ ДЛЯ РЕДАКТИРОВАНИЯ
notepad .env
# Откроется блокнот Windows
```

### Что написать в файле `.env`?

Найди эти строчки и **ЗАМЕНИ** значения:

```env
# 🗄️ Адрес СПЕЦИАЛЬНОЙ базы данных для AR (не путать с основной!)
AR_DATABASE_URL=postgresql://photobooks:YourPassword@localhost:5434/ar_db
#                                        ^^^^^^^^^^^^^^ СМЕНИ на свой пароль!
#                                                               ^^^^ порт 5434 (не 5433!)

# 🔗 Адрес Backend (откуда приходят задачи)
BACKEND_URL=http://localhost:5002
# Это как номер телефона соседней комнаты

# 🔑 Секретный пароль (чтобы только Backend мог связаться)
BACKEND_WEBHOOK_SECRET=my-super-secret-password-12345
#                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Придумай свой!

# 🌐 Адрес Frontend (откуда пользователи заходят)
FRONTEND_URL=http://localhost:3000

# 📁 Где хранить AR файлы
AR_STORAGE_PATH=/app/storage/ar-storage

# ⏰ Когда чистить демо-проекты (2 часа ночи каждый день)
DEMO_CLEANUP_SCHEDULE=0 2 * * *

# 🎨 Включить украшение картинок рамками
AR_ENABLE_BORDER_ENHANCER=true

# 🚪 На каком порту работать
PORT=5000
```

**После редактирования:**
- Нажми `Ctrl+S` (сохранить)
- Закрой блокнот

```powershell
# 4️⃣ ВЕРНУТЬСЯ В ГЛАВНЫЙ ДОМ!
cd ..
# 📍 Снова в: photobooksgallery/ 🏠
```

---

## 🗄️ Шаг 3: Запустить Базы Данных (2 штуки!)

### Где мы?
В **ГЛАВНОМ ДОМЕ** (`photobooksgallery/`)

### Зачем 2 базы данных?
Представь: 
- **Основная БД** (`db-main`, порт 5433) - хранит заказы, пользователей, товары 🛒
- **AR База** (`ar-db`, порт 5434) - хранит только AR проекты 🎮

Они **НЕ МЕШАЮТ** друг другу! Если AR сломается, основной сайт продолжит работать 🛡️

### Что делаем?

```powershell
# 1️⃣ ЗАПУСТИТЬ ТОЛЬКО ДВЕ БАЗЫ ДАННЫХ (не весь проект!)
docker-compose -f docker-compose.ar-microservice.yml up -d db-main ar-db
#                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^     ^^^^^^^ ^^^^^^
#                  Имя файла с инструкциями               Запустить эти 2 контейнера

# Подожди 5-10 секунд... ⏳
# Docker скачивает и запускает базы данных

# 2️⃣ ПРОВЕРИТЬ, ЧТО ЗАПУСТИЛОСЬ
docker-compose -f docker-compose.ar-microservice.yml ps
# Должны быть 2 строчки:
# photobooks-db-main   Up   0.0.0.0:5433->5432/tcp
# photobooks-ar-db     Up   0.0.0.0:5434->5432/tcp
```

### Если что-то не запустилось:

```powershell
# ПОСМОТРЕТЬ ЛОГИ (что случилось)
docker logs photobooks-db-main
docker logs photobooks-ar-db

# ПЕРЕЗАПУСТИТЬ
docker-compose -f docker-compose.ar-microservice.yml restart db-main ar-db
```

**Мы ВСЁ ЕЩЁ в ГЛАВНОМ ДОМЕ!** 🏠 (`photobooksgallery/`)

---

## 🛠️ Шаг 4: Создать Таблицы в AR Базе

### Где мы?
В **ГЛАВНОМ ДОМЕ** (`photobooksgallery/`)

### Зачем?
База данных пустая! Нужно создать "полки" (таблицы), куда будем складывать AR проекты.

### Что делаем?

```powershell
# 1️⃣ ЗАЙТИ В КОМНАТУ AR
cd ar-service
# 📍 Теперь в: photobooksgallery/ar-service/

# 2️⃣ СОЗДАТЬ ТАБЛИЦЫ (полки в базе)
npm run migrate
# Это запустит специальный скрипт, который создаст:
# - ar_projects (информация о AR проектах)
# - ar_compilation_logs (логи что происходило)
# - ar_webhook_events (отслеживание событий)
# - pgboss.* (таблицы для очереди задач)

# Ожидаемый результат:
# ✅ Migration 001_initial_schema.sql executed successfully
# ✅ All migrations completed
```

### Если ошибка:

```powershell
# ПРОВЕРИТЬ, ЧТО БД ЗАПУЩЕНА
docker ps | grep ar-db
# Должна быть строчка: photobooks-ar-db ... Up

# ПРОВЕРИТЬ .ENV (правильный ли пароль?)
Get-Content .env | Select-String "AR_DATABASE_URL"
# Должно быть: AR_DATABASE_URL=postgresql://photobooks:твой_пароль@localhost:5434/ar_db
```

```powershell
# 3️⃣ ВЕРНУТЬСЯ В ГЛАВНЫЙ ДОМ!
cd ..
# 📍 Снова в: photobooksgallery/ 🏠
```

---

## 🚀 Шаг 5: Запустить AR Сервис!

### Где мы?
В **ГЛАВНОМ ДОМЕ** (`photobooksgallery/`)

### Вариант A: Режим Разработки (для тестирования)

```powershell
# 1️⃣ ЗАЙТИ В КОМНАТУ AR
cd ar-service
# 📍 Теперь в: photobooksgallery/ar-service/

# 2️⃣ ЗАПУСТИТЬ СЕРВИС!
npm run dev
# Это запустит сервер, который будет следить за изменениями кода

# Увидишь такой текст:
# 🚀 Starting AR Microservice...
# [Startup] 🔌 Connecting to database...
# [Startup] ✅ Database connected
# [Startup] 📤 Starting pg-boss queue...
# [Startup] ✅ pg-boss started
# [Startup] 👷 Registering workers...
# [Startup] ✅ Registered AR_COMPILE worker (teamSize: 2)
# [Startup] ✅ Registered DEMO_CLEANUP worker
# ✅ AR Microservice running on port 5000

# ⚠️ НЕ ЗАКРЫВАЙ ЭТО ОКНО! Сервис должен работать постоянно
```

**Чтобы остановить:**
- Нажми `Ctrl+C` в окне PowerShell

**Чтобы вернуться в ГЛАВНЫЙ ДОМ (в другом окне PowerShell):**
```powershell
# Открой НОВОЕ окно PowerShell
cd C:\Projects\NextjsBlog\NextjsBlog-broken-backup\photobooksgallery
# 📍 Теперь в ГЛАВНОМ ДОМЕ (в новом окне)
```

### Вариант B: Режим Docker (весь стек сразу)

```powershell
# ЕСЛИ ТЫ В КОМНАТЕ AR - ВЕРНИСЬ СНАЧАЛА!
cd ..
# 📍 Теперь в: photobooksgallery/ 🏠

# ЗАПУСТИТЬ ВСЁ СРАЗУ (frontend + backend + ar-service + базы + nginx)
docker-compose -f docker-compose.ar-microservice.yml up -d

# ПОСМОТРЕТЬ СТАТУС ВСЕХ СЕРВИСОВ
docker-compose -f docker-compose.ar-microservice.yml ps
# Должны быть запущены (Up):
# - photobooks-frontend (port 3000)
# - photobooks-backend (port 5002)
# - photobooks-ar-service (port 5000)
# - photobooks-db-main (port 5433)
# - photobooks-ar-db (port 5434)
# - photobooks-nginx (port 80)

# ПОСМОТРЕТЬ ЛОГИ AR СЕРВИСА
docker logs -f photobooks-ar-service
# Нажми Ctrl+C чтобы выйти из логов
```

**Мы В ГЛАВНОМ ДОМЕ!** 🏠 (`photobooksgallery/`)

---

## ✅ Шаг 6: Проверить, Что Всё Работает

### Где мы?
В **ГЛАВНОМ ДОМЕ** (`photobooksgallery/`) или **КОМНАТЕ AR** (`ar-service/`) - не важно!

### Проверка 1: Health Check

```powershell
# Проверить AR Service (должен ответить "healthy")
curl http://localhost:5000/health

# Ожидаемый ответ:
# {
#   "status": "healthy",
#   "service": "ar-microservice",
#   "database": "connected",
#   "queue": "ok",
#   "uptime": 10.5
# }

# Проверить Backend (если запущен)
curl http://localhost:5002/health
```

### Проверка 2: Открыть в Браузере

```powershell
# Открыть AR Service в браузере
Start-Process "http://localhost:5000/health"

# Открыть Frontend (главный сайт)
Start-Process "http://localhost:3000"
```

---

## 🎮 Как Пользоваться AR Сервисом?

### Шаг 1: Создать AR Проект

```powershell
# Сначала нужно получить токен (авторизоваться на сайте)
# Открой http://localhost:3000 и войди в аккаунт
# Скопируй JWT токен из браузера (инструменты разработчика → localStorage → token)

$token = "твой_jwt_токен_здесь"

# Создать AR проект
Invoke-RestMethod -Uri "http://localhost:5002/api/ar/compile" `
  -Method Post `
  -Headers @{ "Authorization" = "Bearer $token" } `
  -ContentType "application/json" `
  -Body (@{
    photoUrl = "/objects/uploads/test-photo.jpg"
    videoUrl = "/objects/uploads/test-video.mp4"
    isDemo = $true
  } | ConvertTo-Json)

# Ответ будет такой:
# {
#   "projectId": "abc-123-def-456",     ← ВАЖНО! Сохрани этот ID
#   "status": "pending",
#   "estimatedTimeSeconds": 120,        ← Компиляция займёт ~2 минуты
#   "statusUrl": "/status/abc-123-def-456",
#   "viewUrl": "/view/abc-123-def-456"
# }
```

### Шаг 2: Проверить Статус

```powershell
# Подставь свой projectId вместо abc-123-def-456
$projectId = "abc-123-def-456"

# Проверить статус
Invoke-RestMethod -Uri "http://localhost:5002/api/ar/status/$projectId"

# Статусы:
# pending    → В очереди, ещё не начали ⏳
# processing → Компилируется прямо сейчас 🔨
# ready      → Готово! Можно смотреть ✅
# error      → Ошибка компиляции ❌
```

### Шаг 3: Посмотреть AR (когда готово)

```powershell
# Подождать пока status станет "ready" (около 2 минут)
# Потом открыть в браузере:
Start-Process "http://localhost:3000/ar/view/$projectId"

# На телефоне:
# 1. Открой камеру
# 2. Наведи на фото-маркер
# 3. Увидишь 3D видео поверх фото! 🎉
```

---

## 🗺️ Памятка: Где Я Сейчас?

### Как Узнать Где Ты?

```powershell
# Показать текущий путь
pwd
# Ответ может быть:
# C:\Projects\...\photobooksgallery          ← ГЛАВНЫЙ ДОМ 🏠
# C:\Projects\...\photobooksgallery\ar-service  ← КОМНАТА AR 🎮
```

### Навигация (перемещение по комнатам)

```powershell
# ЗАЙТИ В КОМНАТУ AR (из ГЛАВНОГО ДОМА)
cd ar-service
# 📍 Теперь в: photobooksgallery/ar-service/

# ВЕРНУТЬСЯ В ГЛАВНЫЙ ДОМ (из любой комнаты)
cd ..
# 📍 Теперь в: photobooksgallery/ 🏠

# ВЕРНУТЬСЯ В ГЛАВНЫЙ ДОМ (из любого места в проекте)
cd C:\Projects\NextjsBlog\NextjsBlog-broken-backup\photobooksgallery
# 📍 Гарантированно в ГЛАВНОМ ДОМЕ! 🏠

# ЗАЙТИ В КОМНАТУ BACKEND
cd backend
# 📍 Теперь в: photobooksgallery/backend/

# ЗАЙТИ В КОМНАТУ FRONTEND
cd frontend
# 📍 Теперь в: photobooksgallery/frontend/
```

### Правило Большого Пальца 👍

**ВСЕГДА ПОМНИ:**
1. Открыл PowerShell → ты в **ГЛАВНОМ ДОМЕ** 🏠
2. `cd имя_папки` → **ЗАЙТИ** в комнату
3. `cd ..` → **ВЫЙТИ** из комнаты (на 1 уровень вверх)
4. `npm install`, `npm run dev` → выполняются **В ТОЙ КОМНАТЕ, ГДЕ ТЫ СЕЙЧАС**
5. `docker-compose` → выполняется **В ГЛАВНОМ ДОМЕ** (где файл docker-compose.yml)

---

## 🎯 Краткая Шпаргалка (весь процесс)

```powershell
# === ГЛАВНЫЙ ДОМ (photobooksgallery/) ===

# 1. Установить игрушки в AR комнату
cd ar-service                    # Зайти в AR комнату
npm install                      # Установить
cd ..                           # Вернуться в ГЛАВНЫЙ ДОМ

# 2. Настроить секреты
cd ar-service                    # Зайти в AR комнату
Copy-Item .env.example .env      # Скопировать шаблон
notepad .env                     # Отредактировать (не забудь сохранить!)
cd ..                           # Вернуться в ГЛАВНЫЙ ДОМ

# 3. Запустить базы данных
docker-compose -f docker-compose.ar-microservice.yml up -d db-main ar-db

# 4. Создать таблицы
cd ar-service                    # Зайти в AR комнату
npm run migrate                  # Создать таблицы
cd ..                           # Вернуться в ГЛАВНЫЙ ДОМ

# 5. Запустить AR сервис
cd ar-service                    # Зайти в AR комнату
npm run dev                      # Запустить (окно не закрывать!)

# 6. Проверить (в новом окне PowerShell)
curl http://localhost:5000/health   # Должен ответить "healthy"
```

---

## 🆘 Помощь! Что-то Сломалось!

### Проблема: "npm: command not found"
**Решение:** Нужно установить Node.js
```powershell
# Скачай и установи Node.js 18+:
# https://nodejs.org/
```

### Проблема: "docker: command not found"
**Решение:** Нужно установить Docker Desktop
```powershell
# Скачай и установи Docker Desktop:
# https://www.docker.com/products/docker-desktop/
```

### Проблема: "Cannot find module"
**Решение:** Забыл установить зависимости
```powershell
cd ar-service          # Зайти в AR комнату
npm install            # Установить ВСЕ игрушки
cd ..                  # Вернуться
```

### Проблема: "Connection refused" (база недоступна)
**Решение:** База не запущена или не готова
```powershell
# Проверить статус
docker ps | grep ar-db

# Если нет - запустить
docker-compose -f docker-compose.ar-microservice.yml up -d ar-db

# Подождать 10 секунд
Start-Sleep -Seconds 10

# Попробовать снова
cd ar-service
npm run migrate
cd ..
```

### Проблема: "Port 5000 already in use"
**Решение:** Порт занят другой программой
```powershell
# Узнать кто занял порт
netstat -ano | findstr :5000

# Убить процесс (замени 12345 на нужный PID)
taskkill /PID 12345 /F

# Или смени порт в .env:
# PORT=5001
```

---

## 🎉 Поздравляю! Ты Справился!

Теперь у тебя работает:
- ✅ AR Сервис на порту 5000
- ✅ Две базы данных (основная + AR)
- ✅ Очередь задач (pg-boss)
- ✅ Backend НЕ блокируется во время AR компиляции

**Что дальше?**
- Создавай AR проекты через API
- Смотри как они компилируются (2 минуты)
- Открывай на телефоне и показывай друзьям! 📱

**Главное правило:**
> Всегда знай, где ты находишься (`pwd`), и помни, что `cd ..` возвращает тебя назад! 🏠

---

## 📞 Нужна Помощь?

1. Проверь где ты: `pwd`
2. Вернись в ГЛАВНЫЙ ДОМ: `cd ..` (несколько раз если нужно)
3. Посмотри логи: `docker logs photobooks-ar-service`
4. Перечитай эту инструкцию с начала 📖

**Удачи!** 🚀
