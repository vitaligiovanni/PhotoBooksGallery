# Simple Frontend Dockerfile - использует уже собранный dist
FROM nginx:alpine

# Копируем готовый билд
COPY dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/nginx.conf

# Nginx слушает на порту 80
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
