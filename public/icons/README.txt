Generated icons placeholder set.

To regenerate proper sized placeholders (gray gradient) run:
	npm run generate:icons

Replace these with real branded artwork when available. Ensure each file matches its declared resolution in manifest.json.
